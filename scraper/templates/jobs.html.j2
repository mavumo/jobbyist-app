<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Job Listings • Jobbyist Africa</title>
  <link rel="stylesheet" href="https://jobbyist.africa/style.css">
</head>
<body>
  <header><nav><!-- your header HTML here --></nav></header>
  <main>
    <h1>Jobs</h1>
    {% for job in jobs %}
      <div class="job-card {{ 'remote' if job.remote }}">
        <a href="{{ job.link }}" target="_blank">
          <h2>{{ job.title }}</h2>
        </a>
        <div class="meta">
          <span>{{ job.company }}</span> • <span>{{ job.location.text }}</span>
          {% if job.remote %}<span class="tag">Remote</span>{% endif %}
          <span class="posted">{{ job.datePosted.split("T")[0] }}</span>
        </div>
      </div>
      <!-- JSON‑LD -->
      <script type="application/ld+json">
      {{ job | tojson }}
      </script>
    {% endfor %}
  </main>
  <footer><p>&copy; 2025 Jobbyist Africa</p></footer>
</body>
</html>
